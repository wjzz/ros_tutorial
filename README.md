# How to develop using this repo

We are going to use docker.

## Start the container

```
docker compose up -d
```

## Connect to the container

```
docker exec -it ros-dev bash
$ source .bash_profile # inside docker
```
